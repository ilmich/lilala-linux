image sdcard.img {
	hdimage {
		partition-table-type = 'mbr'
	}

	partition boot {
		partition-type = 0xC
		bootable = "true"
		offset = 16M
		image = "boot.vfat"
	}

	partition rootfs {
		image = "rootfs.ext4"
	}
}
